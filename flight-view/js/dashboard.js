'use strict';

const angular = require('angular');

angular.module('flightView')
    .directive('fvMap', () => {
        return {
            restrict: 'E',
            template: 'TODO: make map work'
        }
    });
